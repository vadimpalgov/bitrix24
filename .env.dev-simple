COMPOSE_PROJECT_NAME=bitrix-dev

# www-data uid/gid — должен совпадать с хостом (id www-data)
PUID=33
PGID=33

# MySQL
MYSQL_DATABASE=bitrix
MYSQL_USER=bitrix
MYSQL_PASSWORD=bitrix
MYSQL_ROOT_PASSWORD=root
